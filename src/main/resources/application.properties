# Mobile Testing Configuration
platformName=iOS

# Web Testing Configuration
browser=chrome
# Set to true for CI/CD pipelines (no display needed)
headless=false

# ==================== API Testing Configuration ====================
# Base URL for API requests (optional - can also set in test code)
api.baseUrl=

# ==================== Database Configuration ====================
# Database type: mysql, postgresql, sqlserver
db.type=mysql
db.host=localhost
db.port=3306
db.name=testdb
db.username=root
db.password=